<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="NibbleTools.Views.StringGeneratorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NibbleTools.Views"
    xmlns:vm="using:NibbleTools.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10 10 5 0">
                <Button x:Name="GenerateWord" Content="Generate" Width="100" Height="30" Margin="10 0 5 0" Click="GenerateWord_Click"/>
            <StackPanel Orientation="Horizontal" Margin="10 10 0 0">
                <ComboBox x:Name="DDL" Width="150" Height="30" SelectedIndex="0">
                    <ComboBoxItem Content="Word"/>
                    <ComboBoxItem Content="Sentence"/>
                    <ComboBoxItem Content="Paragraph"/>
                </ComboBox>
            </StackPanel>
                <TextBox x:Name="TextBox1" Width="800" Height="300" Margin="10 20 0 0" IsReadOnly="True" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Page>
